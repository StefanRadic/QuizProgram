1. This program requires that Java is installed (latest jdk / jre) in order to run. Also an execution environment must be set for java in order for this program to run properly.

2. In order to see the exact code of this application, enter the "Java Classes" folder.

3. The JOptionPane unfortunately doesn't have an icon visible on the taskbar. In case if program gets invisible, simply alt+tab to find it.